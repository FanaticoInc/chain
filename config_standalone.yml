mode: client
common:
  data_dir: /home/claude/standalone_data
  log:
    format: JSON
    level:
      default: info
      cometbft: info
      cometbft/context: error

p2p:
  port: 26658

genesis:
  file: /home/claude/genesis_fixed_final.json